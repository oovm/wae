# tooling

工程脚本与隔离内容。应用作者通常**不需要**读这里；请从仓库根 [`readme.md`](../../readme.md) 进入。

| 路径 | 说明 |
|------|------|
| [`_quarantine/`](_quarantine/readme.md) | 历史代码隔离；非官方 API |
| 仓库根 `scripts/` | `check-boundary` / `check-ts` / `publish` / `format` 等 |

不要把 quarantine 里的包当作稳定依赖。
