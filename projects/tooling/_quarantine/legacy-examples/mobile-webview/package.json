{
    "name": "@wae-example/mobile-webview",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "node -e \"console.error('${ex.id}: 平台尚未实现，仅保留契约'); process.exitCode = 1\"",
        "build": "node -e \"console.error('${ex.id}: 平台尚未实现'); process.exitCode = 1\"",
        "check": "tsc -p tsconfig.json --noEmit",
        "test": "node -e \"console.log('contract skeleton')\""
    },
    "dependencies": {
        "@wae/client": "workspace:*",
        "@wae/wae-android-arm64": "workspace:*",
        "@wae/wae-ios-arm64": "workspace:*"
    },
    "devDependencies": {
        "@wae/wae": "workspace:*",
        "typescript": "catalog:"
    }
}
