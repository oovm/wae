# _quarantine

架构删除的隔离区（非 workspace 包，不发布）。

- `legacy-ui`：原 `@wae/ui`（组件库已从 WAE 核心移除）
- `legacy-jsx`：原 client JSX / ViewNode / signals
- `legacy-examples`：按「自有 UI」组织的旧示例

人工确认后可删除；reshape 不递归清空用户工作区。
