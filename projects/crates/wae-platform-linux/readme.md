# `wae-platform-linux`

Linux desktop binding for WAE (direct **WebKitGTK** + **GTK**; no tao/wry).

**Status (0.1.0):** skeleton only — `run_desktop` returns `PlatformError::Unsupported` until WebKitGTK wiring lands.

```bash
cargo check -p wae-platform-linux
```
