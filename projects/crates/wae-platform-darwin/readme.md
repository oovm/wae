# `wae-platform-darwin`

macOS desktop binding for WAE (direct **WKWebView** + **AppKit**; no tao/wry).

**Status (0.1.0):** skeleton only — `run_desktop` returns `PlatformError::Unsupported` until WKWebView wiring lands.

```bash
cargo check -p wae-platform-darwin
```
